import { useEffect, useMemo, useRef, useState } from 'react';
import { cards, type CardData } from './data/gameCards';
import { beginTurn, momentumLabel, playUnit, resolveCombat, resolveEndTurn, type BattleSideState, type BattleState } from './data/battleEngine';
import { synergyProgress, synergies } from './data/synergies';
import { clearBattle, emptyBattleStats, finishBattle, loadBattle, saveBattle, type SavedBattle } from './data/battleSession';

const generatedBase='/assets/generated/';
const battlefield=`${generatedBase}chatgpt-image-6-sept.-2026-01_35_40.webp`;
const familySheet:Record<string,string>={Magiciens:'chatgpt-image-7-sept.-2026-15_03_35.webp',Ombres:'chatgpt-image-7-sept.-2026-15_03_31.webp',Nobles:'chatgpt-image-7-sept.-2026-15_07_01.webp',Robots:'chatgpt-image-7-sept.-2026-15_01_47.webp',Nature:'chatgpt-image-7-sept.-2026-22_07_59.webp',Guérisseurs:'chatgpt-image-7-sept.-2026-15_03_15.webp',Créatures:'chatgpt-image-7-sept.-2026-15_04_45.webp'};
type Phase='intro'|'player'|'enemy'|'combat'|'result';type Result='win'|'lose'|null;
function rc(r:string){return r.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function artFor(card:CardData){return card.assetPath||`${generatedBase}${familySheet[card.family]||'carte-bleue.webp'}`}
function emptyBoard(){return Array(7).fill(null)}
function shuffle<T>(input:T[]){const a=[...input];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}
function boardCount(side:BattleSideState){return side.board.filter(Boolean).length}
function createSide(pool:CardData[]):BattleSideState{const deck=shuffle(pool);return {board:emptyBoard(),heroHp:20,energy:5,hand:deck.slice(0,5),deck:deck.slice(5),shield:0,momentum:0,survivalUsed:false,pirateDrawUsed:false,healerSaveUsed:false,firstPlayDone:false}}
function createBattle(playerPool:CardData[],enemyPool:CardData[]):SavedBattle{const state:BattleState={player:createSide(playerPool),enemy:createSide(enemyPool),turn:1,log:['Le duel commence.']};return {version:2,startedAt:Date.now(),updatedAt:Date.now(),maxMomentum:0,stats:emptyBattleStats(),state}}
function drawOne(side:BattleSideState){if(side.hand.length>=5||!side.deck[0])return side;return {...side,hand:[...side.hand,side.deck[0]],deck:side.deck.slice(1)}}
function formatDuration(ms:number){const total=Math.max(0,Math.round(ms/1000)),m=Math.floor(total/60),s=total%60;return `${m} min ${String(s).padStart(2,'0')} s`}

export function BattleArena({playerPool,onWin,onLose}:{playerPool:CardData[];onWin:()=>void;onLose:()=>void}){
 const enemyPool=useMemo(()=>cards.filter(c=>c.type==='Héros').slice(8,36),[]);const initial=useMemo(()=>loadBattle()||createBattle(playerPool,enemyPool),[playerPool,enemyPool]);
 const[saved,setSaved]=useState<SavedBattle>(initial),[phase,setPhase]=useState<Phase>(()=>loadBattle()?'player':'intro'),[selected,setSelected]=useState<string|null>(null),[result,setResult]=useState<Result>(null),[banner,setBanner]=useState('Prépare ton premier tour');const rewarded=useRef(false);const state=saved.state;
 const playerSyn=synergies(state.player,state.enemy),enemySyn=synergies(state.enemy,state.player),playerProgress=synergyProgress(state.player),enemyProgress=synergyProgress(state.enemy);
 useEffect(()=>{if(phase!=='result')saveBattle(saved)},[saved,phase]);
 useEffect(()=>{if(state.player.heroHp<=0&&!result)endMatch('lose');else if(state.enemy.heroHp<=0&&!result)endMatch('win')},[state.player.heroHp,state.enemy.heroHp,result]);
 function patchState(next:BattleState,extra?:Partial<SavedBattle>){const maxMomentum=Math.max(saved.maxMomentum,next.player.momentum);setSaved({...saved,...extra,maxMomentum,state:next,updatedAt:Date.now()})}
 function start(){let player=beginTurn(state.player,state.enemy);player=drawOne(player);patchState({...state,player,log:['Tour 1 : à toi de jouer.']});setPhase('player');setBanner('À toi de jouer')}
 function select(card:CardData){if(phase!=='player')return;setSelected(selected===card.id?null:card.id)}
 function place(slot:number){
   if(phase!=='player'||!selected)return;
   const card=state.player.hand.find(c=>c.id===selected);if(!card)return;
   const before=synergyProgress(state.player);const nextPlayer=playUnit(state.player,state.enemy,card,slot);if(nextPlayer===state.player)return;
   const after=synergyProgress(nextPlayer);const unlocked=after.find(x=>x.tier>(before.find(b=>b.family===x.family)?.tier??0));
   const nextStats={...saved.stats,cardsPlayed:saved.stats.cardsPlayed+1,synergyActivations:saved.stats.synergyActivations+(unlocked?1:0),maxBoard:Math.max(saved.stats.maxBoard,boardCount(nextPlayer)),comebackTriggered:saved.stats.comebackTriggered||nextPlayer.heroHp<=10};
   patchState({...state,player:nextPlayer,log:[unlocked?`${unlocked.family} atteint le palier ${unlocked.tier===2?'5/5':'3/3'} : ${unlocked.label} activée.`:`${card.name} entre en jeu sur la ligne ${slot+1}.`,...state.log].slice(0,5)},{stats:nextStats});
   setSelected(null);setBanner(unlocked?`${unlocked.label} activée`:`${card.name} est en position`)
 }
 function enemyTurn(){setPhase('enemy');setBanner('Le rival prépare sa réponse');setTimeout(()=>{let enemy=beginTurn(state.enemy,state.player);enemy=drawOne(enemy);const empties=enemy.board.map((u,i)=>u?-1:i).filter(i=>i>=0);const affordable=enemy.hand.filter(c=>c.cost<=enemy.energy);if(affordable.length&&empties.length){const scored=[...affordable].sort((a,b)=>{const ac=enemy.board.filter(x=>x?.family===a.family).length,bc=enemy.board.filter(x=>x?.family===b.family).length;const aNear=(ac===2||ac===4)?3:0,bNear=(bc===2||bc===4)?3:0;return (bNear+b.cost)-(aNear+a.cost)});const card=scored[0];const openThreat=empties.find(i=>!!state.player.board[i]);const slot=openThreat??empties[0];enemy=playUnit(enemy,state.player,card,slot)}const staged={...state,enemy,log:['Le rival a terminé son placement.',...state.log].slice(0,5)};patchState(staged);setPhase('combat');setBanner('Résolution du combat');setTimeout(()=>resolveRound(staged),650)},650)}
 function resolveRound(current:BattleState){
   const beforePlayerHp=current.player.heroHp,beforeEnemyHp=current.enemy.heroHp;
   const clash=resolveCombat(current.player,current.enemy);const end=resolveEndTurn(clash.attacker,clash.defender);
   const afterPlayerHp=end.side.heroHp,afterEnemyHp=end.enemy.heroHp;
   let player=drawOne(end.side),enemy=drawOne(end.enemy);const nextTurn=current.turn+1;player=beginTurn(player,enemy);enemy=beginTurn(enemy,player);
   const damageDealt=Math.max(0,beforeEnemyHp-afterEnemyHp),damageTaken=Math.max(0,beforePlayerHp-afterPlayerHp),healingDone=Math.max(0,afterPlayerHp-beforePlayerHp);
   const comebackNow=player.heroHp<=10||player.survivalUsed||player.momentum>=3;
   const nextStats={...saved.stats,unitsDestroyed:saved.stats.unitsDestroyed+clash.attackerKills,unitsLost:saved.stats.unitsLost+clash.defenderKills,damageDealt:saved.stats.damageDealt+damageDealt,damageTaken:saved.stats.damageTaken+damageTaken,healingDone:saved.stats.healingDone+healingDone,comebackTriggered:saved.stats.comebackTriggered||comebackNow,maxBoard:Math.max(saved.stats.maxBoard,boardCount(player))};
   const line=clash.attackerKills||clash.defenderKills?`${clash.attackerKills} unité adverse éliminée, ${clash.defenderKills} unité alliée perdue.`:'Le front tient. Aucun combattant n’est tombé.';
   const next:BattleState={player,enemy,turn:nextTurn,log:[line,`Tour ${nextTurn} : à toi de jouer.`,...current.log].slice(0,5)};
   patchState(next,{stats:nextStats});setSelected(null);setPhase('player');setBanner(`Tour ${nextTurn}`)
 }
 function endMatch(value:'win'|'lose'){if(rewarded.current)return;rewarded.current=true;setResult(value);setPhase('result');setSelected(null);const finalized={...saved,state};finishBattle(finalized,value);value==='win'?onWin():onLose();setBanner(value==='win'?'Victoire':'Défaite')}
 function newMatch(){clearBattle();rewarded.current=false;const fresh=createBattle(playerPool,enemyPool);setSaved(fresh);setResult(null);setSelected(null);setPhase('intro');setBanner('Prépare ton premier tour')}
 const duration=Date.now()-saved.startedAt;
 return <section className={`battle-screen cinematic-battle phase-${phase}`} style={{backgroundImage:`linear-gradient(rgba(4,9,20,.14),rgba(4,9,20,.38)),url('${battlefield}')`}}><div className="battle-vignette"/><div className="battle-header-panel"><PlayerStatus name="Rival" hp={state.enemy.heroHp} energy={state.enemy.energy} shield={state.enemy.shield} momentum={state.enemy.momentum} side="enemy"/><div className="turn-center"><span className="turn-label">Tour</span><strong>{state.turn}</strong><span className="phase-label">{banner}</span></div><PlayerStatus name="Toi" hp={state.player.heroHp} energy={state.player.energy} shield={state.player.shield} momentum={state.player.momentum} side="player"/></div><ProgressStrip title="Progression adverse" items={enemyProgress}/><SynergyStrip title="Synergies adverses" items={enemySyn}/><div className="enemy-zone battle-row">{state.enemy.board.map((u,i)=><ArenaSlot key={i} unit={u} index={i} side="enemy"/>)}</div><div className="arena-center cinematic-center"><div className={`phase-banner ${phase}`}>{banner}</div><div className="battle-log-panel">{state.log.slice(0,3).map((line,i)=><p key={`${line}-${i}`}>{line}</p>)}</div></div><div className="player-zone battle-row">{state.player.board.map((u,i)=><ArenaSlot key={i} unit={u} index={i} side="player" target={!!selected&&phase==='player'} onClick={()=>place(i)}/>)}</div><ProgressStrip title="Progression des familles" items={playerProgress}/><SynergyStrip title="Tes synergies" items={playerSyn}/><div className="battle-command-bar"><div className="hand-title"><span>Main</span><small>{state.player.hand.length}/5 cartes</small></div><div className="hand cinematic-hand">{state.player.hand.map(c=><button key={c.id} className={`hand-card ${selected===c.id?'selected':''}`} onClick={()=>select(c)} disabled={phase!=='player'||c.cost>state.player.energy}><BattleCard card={c} hp={c.hp??1}/></button>)}</div><button className="end-turn-button" onClick={enemyTurn} disabled={phase!=='player'}>Terminer le tour</button></div>{phase==='intro'&&<div className="battle-overlay intro-overlay"><div className="battle-modal"><span className="modal-kicker">Arène classée</span><h2>Le duel commence</h2><p>Construis tes synergies à 3 puis 5 unités. Garde de l’énergie pour préparer un retournement de partie.</p><div className="intro-stats"><span>20 PV</span><span>5 énergie</span><span>7 lignes</span></div><button className="primary" onClick={start}>Entrer dans l’arène</button></div></div>}{result&&<div className="battle-overlay result-overlay"><div className={`battle-modal result-card ${result}`}><span className="modal-kicker">Fin de partie</span><h2>{result==='win'?'Victoire dans l’arène':'Le rival remporte le duel'}</h2><p>{result==='win'?'Récompenses : 80 pièces et 60 XP.':'Analyse ton placement, modifie ton deck et relance un duel.'}</p><div className="result-stats detailed"><span><small>Tours</small>{state.turn}</span><span><small>Durée</small>{formatDuration(duration)}</span><span><small>PV restants</small>{state.player.heroHp}</span><span><small>Dégâts infligés</small>{saved.stats.damageDealt}</span><span><small>Unités éliminées</small>{saved.stats.unitsDestroyed}</span><span><small>Synergies activées</small>{saved.stats.synergyActivations}</span><span><small>Momentum max</small>{saved.maxMomentum}/5</span><span><small>Cartes jouées</small>{saved.stats.cardsPlayed}</span><span><small>Retournement</small>{saved.stats.comebackTriggered?'Déclenché':'Non déclenché'}</span></div><button className="primary" onClick={newMatch}>Nouvelle partie</button></div></div>}</section>
}
function PlayerStatus({name,hp,energy,shield,momentum,side}:{name:string;hp:number;energy:number;shield:number;momentum:number;side:'player'|'enemy'}){return <div className={`status-card ${side}`}><div className="status-name"><strong>{name}</strong><span>{momentumLabel(momentum)}</span></div><div className="status-bars"><Meter label="PV" value={hp} max={20}/><Meter label="Énergie" value={energy} max={8}/><Meter label="Momentum" value={momentum} max={5}/></div>{shield>0&&<span className="shield-badge">Bouclier {shield}</span>}</div>}
function Meter({label,value,max}:{label:string;value:number;max:number}){return <div className="meter"><div className="meter-label"><span>{label}</span><strong>{value}</strong></div><div className="meter-track"><span style={{width:`${Math.max(0,Math.min(100,value/max*100))}%`}}/></div></div>}
function ProgressStrip({title,items}:{title:string;items:{family:string;count:number;next:number|null;tier:number;label:string}[]}){return <div className="progress-strip"><span className="synergy-strip-title">{title}</span>{items.length?items.slice(0,5).map(x=><div className={`progress-chip tier-${x.tier}`} key={x.family}><strong>{x.family}</strong><span>{x.next?`${x.count}/${x.next}`:`5/5`}</span><small>{x.tier===0?'Palier I':x.tier===1?'Palier II':x.label}</small></div>):<span className="synergy-empty">Aucune famille en jeu</span>}</div>}
function SynergyStrip({title,items}:{title:string;items:{id:string;title:string;description:string;tier:number}[]}){return <div className="synergy-strip"><span className="synergy-strip-title">{title}</span>{items.length?items.slice(0,4).map(s=><div className={`synergy-chip tier-${s.tier}`} key={s.id}><strong>{s.title}</strong><span>{s.description}</span></div>):<span className="synergy-empty">Aucune synergie active</span>}</div>}
function ArenaSlot({unit,index,side,target,onClick}:{unit:BattleSideState['board'][number];index:number;side:'player'|'enemy';target?:boolean;onClick?:()=>void}){const content=unit?<BattleCard card={unit} hp={unit.currentHp??unit.hp??1}/>:<span className="slot-number">{String(index+1).padStart(2,'0')}</span>;return side==='player'?<button className={`arena-slot ${target&&!unit?'target':''} ${unit?'occupied':''}`} onClick={onClick} disabled={!target&&!!onClick}>{content}</button>:<div className={`arena-slot enemy ${unit?'occupied':''}`}>{content}</div>}
function BattleCard({card,hp}:{card:CardData;hp:number}){return <article className={`battle-card ${rc(card.rarity)}`}><div className="battle-card-top"><span className="card-cost">{card.cost}</span><span className="card-family">{card.family}</span></div><div className="battle-card-art"><img src={artFor(card)} alt={card.name} onError={e=>{(e.currentTarget as HTMLImageElement).src=`${generatedBase}${familySheet[card.family]||'carte-bleue.webp'}`}}/></div><div className="battle-card-copy"><strong>{card.name}</strong><small>{card.rarity}</small></div><div className="battle-card-stats"><span><small>ATQ</small>{card.atk??0}</span><span><small>PV</small>{hp}</span></div></article>}
