import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pawclaw.game',
  appName: 'Paw & Claw',
  webDir: 'dist',
  backgroundColor: '#081322',
};

export default config;
